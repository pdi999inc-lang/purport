{
  "name": "purport",
  "version": "0.1.0",
  "description": "PurPort — marketplace deal-intelligence: screenshot in, scam-probability + fair-price verdict out.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.22.2",
    "multer": "^1.4.5-lts.1"
  }
}
